---
description: When modifying index.html or project entry points
globs: index.html
---

- `index.html` is the Vite React entry point. Never overwrite it with standalone HTML/D3 content.
- Standalone HTML apps (`standalone.html`, `comprehensive-stemma.html`) are separate implementations — do not mix them with the React app's entry point.
